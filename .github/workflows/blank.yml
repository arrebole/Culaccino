
name: CI
on: [push]

jobs:
    build:
      name: build
      runs-on: ubuntu-latest
      steps:

      - name: Set up Go 1.13
        uses: actions/setup-go@v1
        with:
          go-version: 1.13

      - name: Go build
        run: |
          echo "start build ..."
          go build