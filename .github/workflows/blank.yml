
name: CI
on: [push]

jobs:
    build:
      name: build
      runs-on: ubuntu-latest
      steps:

      - name: Set up Go 1.14
        uses: actions/setup-go@v1
        with:
          go-version: 1.14
        id: go

      - name: Go build
        run: |
          echo "start build ..."
          go build